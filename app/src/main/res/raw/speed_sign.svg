<svg width="200" height="200" xmlns="http://www.w3.org/2000/svg">
    <defs>
        <style type="text/css">
            @font-face {
                font-family: 'speedsignfont';
                src: url('file:///android_res/font/caracteres_l1.ttf');
            }
        </style>
    </defs>
    <!-- Cercle extérieur rouge -->
    <circle cx="100" cy="100" r="90" fill="white" stroke="red" stroke-width="20"/>

    <!-- Texte de la limitation de vitesse -->
    <text id="speed" x="100" y="125" font-family="@font/speedsignfont" font-size="70" fill="black" text-anchor="middle">0</text>
</svg>